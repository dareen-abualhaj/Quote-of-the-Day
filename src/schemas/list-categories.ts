import * as z from "zod/v4";

/**
 * Schema for tool: list_categories
 * Description: Fetches all available categories/tags for quote filtering.
 */
export const ListCategoriesInputSchema = z.object({});
